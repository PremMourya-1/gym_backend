require("dotenv").config(); const Razorpay = require("razorpay"); const rzp = new Razorpay({ key_id: process.env.RAZORPAY_KEY_ID, key_secret: process.env.RAZORPAY_KEY_SECRET }); console.log("ID=", process.env.RAZORPAY_KEY_ID); console.log("SECRET=", process.env.RAZORPAY_KEY_SECRET ? "***" : "missing"); rzp.orders.create({ amount: 100, currency: "INR", receipt: "test_rcpt" }).then(order => { console.log("order created", JSON.stringify(order)); }).catch(err => { console.error("error", err); process.exit(1); });
